const {extractFormData} = require('../helper/helperFunctions')

exports.accessRegulator = (req, res) => {
    const formData = extractFormData(req)

    
}

const verifyNanopub = (np) => {
    return new Promise()
}